# Shaderbox

Pixel Potion — a visual shader playground. Build recipes from math steps and preview them live in the browser.

## Develop

```sh
cd web
npm install
npm run dev
```

## Build

```sh
cd web
npm run build
```

Deploys to Netlify via `@sveltejs/adapter-netlify` (prerendered, `ssr = false`).
